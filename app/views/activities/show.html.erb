<div class="mx-auto md:w-2/3 w-full flex">
  <div class="mx-auto">
    <% if notice.present? %>
      <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
    <% end %>

    <%= render @activity %>

    <%= link_to "Edit this activity", edit_activity_path(@activity), class: "mt-2 rounded-lg py-1.5 px-2.5 bg-gray-100 inline-block font-medium" %>
    <%= link_to "Back to activities", activities_path, class: "ml-2 rounded-lg py-1.5 px-2.5 bg-gray-100 inline-block font-medium" %>
    <div class="inline-block">
      <%= button_to "Destroy this activity", @activity, method: :delete, data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'}, class: "mt-2 rounded-lg py-1.5 px-2.5 bg-gray-100 font-medium" %>
    </div>
  </div>
</div>
